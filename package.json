{
  "name": "bdsk-validator",
  "version": "0.4.0",
  "description": "Reference validator for the BDSK v0.3 specification",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "bdsk-validate": "dist/cli.js"
  },
  "scripts": {
    "build": "tsc && bun run bundle",
    "bundle": "bun build hooks/validate-single-artifact.js --outfile dist/validate-single-artifact.bundle.js --target node && bun build dist/cli.js --outfile dist/cli.bundle.js --target node",
    "dev": "tsc --watch",
    "test": "bun test",
    "lint": "tsc --noEmit",
    "validate": "bun run dist/cli.js"
  },
  "keywords": ["bdsk", "specification", "governance", "validator", "bdd"],
  "license": "MIT",
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.8"
  },
  "dependencies": {
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1",
    "yaml": "^2.7.1"
  }
}
