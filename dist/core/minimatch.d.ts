/**
 * Simple glob pattern matching for file paths.
 * Supports: * (any segment chars), ** (any path segments), ? (single char)
 */
export declare function minimatch(path: string, pattern: string): boolean;
//# sourceMappingURL=minimatch.d.ts.map